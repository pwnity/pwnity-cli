{
  "name": "gobuster",
  "path": "/usr/bin/gobuster",
  "sudo": false,
  "commands": [
    {
      "name": "dir",
      "params": [
        "dir",
        "-w $wordlist.path",
        "-u $target.base_url"
      ]
    },
    {
      "name": "dns",
      "params": [
        "dns",
        "-w $wordlist.path",
        "--wildcard",
        "-d $target.domain"
      ]
    }
  ],
  "description": "Performs directory, DNS, and virtual host brute-forcing against target services"
}